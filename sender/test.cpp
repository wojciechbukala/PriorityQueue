#include <iostream>
//#include <bits/stdc++.h>
#include "Stack.hpp"



using namespace std;

/*
std::string convertToString(char* a)
{
    std::string s = a;
    return s;
} */

int main()
{
/*
    Stack<std::string> e;
    e.push("Wojt");
    e.push("Mati");
    e.push("Janu");
    e.push("Marc");
    e.view();
    cout << "Size:" << e.size() << endl; 
    e.remove();
    e.remove();
    e.view();
    cout << "Size:" << e.size() << endl; 
    e.remove();
    e.remove();
    //e.remove();
    e.view();
*/
    char a[4] = {'a', 'b', 'c', 'd'};
    std::string s_a = a;
    cout << s_a <<endl;
        return 0;
}